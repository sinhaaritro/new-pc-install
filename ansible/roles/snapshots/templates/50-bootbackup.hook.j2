[Trigger]
Operation=Install
Operation=Upgrade
Operation=Remove
Type=Path
Target=boot/*

[Action]
Depends=rsync
Description=Backing up /boot
When=PreTransaction
Exec=/bin/sh -c 'mkdir -p /.bootbackup && rsync -a --delete /boot /.bootbackup'
