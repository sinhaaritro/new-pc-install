---
# Variant-selected: inventory audio_variant names the x_packages.audio key; ~ disables the role.
- name: Install audio x_packages ({{ audio_variant }})
  ansible.builtin.include_role:
    name: install_packages
  vars:
    install_packages_source: "{{ x_packages.audio[audio_variant] }}"
