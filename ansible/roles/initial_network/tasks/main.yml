---
- name: Install NetworkManager in chroot
  ansible.builtin.command: >
    chroot {{ target_root }}
    pacman -S --noconfirm --needed networkmanager
  changed_when: true

- name: Enable NetworkManager in chroot
  ansible.builtin.command: >
    chroot {{ target_root }}
    systemctl enable NetworkManager
  changed_when: true
