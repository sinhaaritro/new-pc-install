---
# Variant-selected per ADR-018: the role ships every helper implementation and
# inventory `aur_variant` names one. `aur_variant: ~` makes play 02 skip
# the role entirely.
- name: Include AUR helper variant
  ansible.builtin.include_tasks: "helpers/{{ aur_variant }}.yml"
  when: aur_variant is not none
