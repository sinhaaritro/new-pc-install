---
# Vendor-specific tasks: intel (loaded from main.yml via include_tasks).
# Not implemented yet — the main.yml gate should prevent reaching here.
# When implemented, add tasks following the nvidia.yml pattern (chroot
# context, {{ mount_point }} file paths, arch-chroot for commands).

- name: Intel GPU setup not implemented (module nvidia)
  ansible.builtin.debug:
    msg: >-
      gpu_vendor=intel is declared but not implemented yet.
      No Intel GPU driver was installed.
